const addCount = () => {
    return { type: "add" }
}

const minusCount = () => {
    return { type: "minus" }
}

const umnoCount = () => {
    return { type: "umno" }
}

const deliCount = () => {
    return { type: "deli" }
}

export {addCount, minusCount, umnoCount, deliCount}